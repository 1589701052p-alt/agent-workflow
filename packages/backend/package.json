{
  "name": "@agent-workflow/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/main.ts",
  "bin": {
    "agent-workflow": "src/main.ts"
  },
  "scripts": {
    "dev": "bun run --watch src/main.ts start",
    "start": "bun run src/main.ts start",
    "build": "echo 'use scripts/build-binary.ts (M5)'",
    "lint": "eslint src tests --max-warnings=0",
    "lint:fix": "eslint src tests --fix",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "db:generate": "drizzle-kit generate",
    "perf:sweep": "bun run scripts/perf-sweep.ts"
  },
  "dependencies": {
    "@agent-workflow/shared": "workspace:*",
    "@modelcontextprotocol/sdk": "1.27.1",
    "drizzle-orm": "^0.36.4",
    "fflate": "^0.8.2",
    "hono": "^4.6.14",
    "jose": "^6.2.3",
    "ulid": "^2.3.0",
    "yaml": "^2.6.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.30.0",
    "fast-check": "^3.23.0"
  }
}
